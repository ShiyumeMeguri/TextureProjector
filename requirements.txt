# TextureProjector 2.0 has NO required external dependencies.
#
# The Gemini REST API is called through Python's standard library
# (urllib / json / base64), all image pixel handling goes through
# Blender's own image API + numpy (bundled with Blender).
#
# Nothing to pip-install. This file is kept only for documentation.
