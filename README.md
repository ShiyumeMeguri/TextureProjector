# TextureProjector (Gemini AI Blender Addon) 🎨🚀

**TextureProjector** 是一款基于 Google Gemini AI 的新时代贴图工具。抛弃繁琐的 Tag 堆砌，直接通过自然语言描述即可为 3D 模型生成高质量贴图。

## 🆕 v2.0 重构说明

- **API 失效已修复**: 默认模型改为免费的 `gemini-3.1-flash-image-preview`，并按模型家族自动选择 responseModalities / imageConfig（400 时自动降级重试），旧的 `gemini-2.5-flash-image` 默认值失效问题不复存在。
- **零依赖**: REST 调用全部走 Python 标准库（urllib），不再需要 pip 安装 google-genai / Pillow / requests，也不再在启动时自动装包。
- **一键化**: 物体模式下选中网格直接投影全部面；编辑模式下只投影选中面。API Key 只存插件偏好设置（不再写进 .blend）。
- **性能**: UV 投影 / 深度归一化 / 像素处理全部 numpy 向量化（foreach_get/foreach_set），移除了逐顶点 Python 循环。
- **画廊缩略图**: Projection Gallery 现在显示每次生成的缩略图。
- **烘焙不再污染场景**: 烘焙后完整恢复 Cycles 采样数等所有被修改的渲染设置。

## ⚠️ Known Issues

1. **已修复(v2.0): 视角对齐问题**: 无相机时现在会根据视口矩阵自动构建一个临时虚拟相机，颜色/深度捕捉与 UV 投影全部使用同一套相机矩阵渲染，无相机也能像素级对齐。
2. **已修复: 切换其他 blend 后烘焙变黑**: 之前存在 Blender 启动后第一次投影烘焙正常，但切换到其他 `.blend` 文件后投影结果会变黑、必须重启 Blender 的问题。这个生命周期和清理时序问题已经修复，现在切换文件后可以继续正常投影和烘焙。
3. **已修复(v2.0): 骨架姿势投影不对齐**: UV 投影现在优先使用 depsgraph 评估后的（变形后）顶点坐标（拓扑一致时），骨架摆姿势后投影与渲染捕捉保持一致；仅当修改器改变了顶点数时才回退到静置坐标。

## ✨ 核心功能

- **自然语言驱动**: 像和人说话一样描述需求，AI 自动理解光影、材质与艺术风格。
- **智能视角还原**: 自动记录每一张历史贴图的相机状态，支持一键“瞬移”回视角进行重投影。
- **高保真自动化烘焙**: 深度集成 Blender 原生烘焙引擎，自动优化色彩空间（Standard Transform）与采样，杜绝抖动生成的噪点。
- **多样化生成引导**: 支持深度图 (Depth)、视角颜色 (Color) 或自定义图片 (Image) 作为生成源 这样即使没有apikey 也可以手动用Debug模式输出截图去网页版banana编辑修改。

## 🚀 快速上手

1. **安装**: 在 Blender 插件设置中安装 ZIP。
2. **配置**: 在侧边栏 `Gemini` 面板填入 [API Key](https://aistudio.google.com/api-keys/)。
3. **投影**: 
    - 选中模型并进入**编辑模式**，选中目标面。
    - 在面板中输入自然语言描述（例：“老旧且生锈的重型工业金属板”）。
    - 点击 **AI Texture Projection** 等待生成与投射。
4. **烘焙**: 
    - 确保面板中的 **Bake Result to Original UVs** 已勾选，投射完成后会自动烘焙到模型 UV 贴图上。
5. **历史还原**:
    - 在 `Projection Gallery` 中点击图片旁的齿轮图标，选择 **Use as Projection Source**，视角会自动还原到当初渲染时的位置进行完美重投影。

---

## 💡 风格模板 (Style Templates)

> 🚧 **正在收集好用的风格模板与提示词！** 未来计划实现游戏风格一键切换功能。  
> 欢迎提交 PR 分享你的模板！

目前随便收集的:

BV19FR1BHETb 将图1的头发、服装、头发装饰、脸型、瞳孔颜色和形状、兽耳替换成图2的头发、服装、头发装饰、脸型、瞳孔颜色和形状、兽耳，要求修改后的头发、服装、头发装饰、脸型、瞳孔颜色和形状、兽耳和图2一模一样，保持图1构图不变，只取图2头部特写，保持图1的背景不变，保持图1的3d渲染风格不变，保持图1的皮肤颜色风格。

### 模板格式规范

提交时请按以下格式：

```
| 风格名称 | 输入图 | 输出图 | 提示词 |
```

> 💡 **提示词技巧**: 尽量使用通用、可迁移的描述，避免特定模型细节。

---
*Powered by Advanced Agentic Coding.*
